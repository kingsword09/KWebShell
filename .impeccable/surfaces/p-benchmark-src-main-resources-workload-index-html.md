---
version: 1
slug: "p-benchmark-src-main-resources-workload-index-html"
primary_target: "kweb-example-app-benchmark/src/main/resources/workload/index.html"
related_targets: []
---

Scope: the repository-local application-scale benchmark workload at `/index.html`.
Mode: Operate.

Audience and job: KWebShell engine maintainers run a deterministic AI-workbench scenario, inspect visible state, and collect browser/host performance evidence without mistaking synthetic workload content for product claims.

Primary task: exercise History API navigation, streaming message rendering, Markdown/code output, virtualization, workers, IndexedDB, WebSocket traffic, media decode, DevTools, and performance observers through one coherent application shell. The visible “Run proof” control performs the same deterministic scenario invoked through CDP.

Direction: prepress proofing desk. A coated-paper light field, graphite type, narrow CMYK registration strip, job-ticket navigation, galley-like conversation stream, measuring rules, and explicit textual status. Duration is expressed spatially in the proof timeline; streaming text is the principal motion. Avoid generic AI chat cards, decorative glass, and color-only state.

First viewport: a compact job-ticket rail at left; registration/header controls across the top; a wide proof galley in the center; an inspection ledger at right; timeline/ruler across the bottom. The synthetic prompt and streamed proof are already visible, with the “Run proof” action in the header.

Memorable moment: the proof galley receives live composed text while CMYK registration marks advance across the ruler, making workload progress visible without an overlay.

Constraints: semantics and keyboard focus remain first class; desktop is the benchmark viewport and mobile remains usable for visual review; all content is explicitly synthetic; reduced-motion removes animated travel while retaining state changes; seed `061646a2`.
