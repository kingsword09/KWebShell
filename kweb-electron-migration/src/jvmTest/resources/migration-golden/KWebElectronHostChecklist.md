# KWebShell Migration Host Checklist: io.github.kwebshell.migration.fixture

Generated by KWebShell Electron migration codegen. Deterministic across all platforms.

Renderer origin: `app://migration-fixture`; profile: `default`.

## 1. Required KMP Native Services
- [ ] Install `app-paths` (version `1.0.0`)

## 2. Window & Hierarchy Definitions
- [ ] Window `main`: "KWebShell Migration Fixture" [Main] (Profile: `default`)
  - [ ] Register immutable parent/modality and call `setFullscreen(WINDOWED)` from host code.
  - [ ] Apply typed capabilities: closable=true, movable=true, minimizable=true, maximizable=true, resizable=true, alwaysOnTop=false.

## 3. Profiles
- [ ] Profile `default` (persistent, storage: `profiles/default`)

## 4. Preload Methods & Streams
- [ ] Preload method `getPath` -> Channel `app.getPath` (Status: `ADAPTER`)
- [ ] Stream `progress` -> Method `downloadProgress` (Capacity: `32`, Status: `ADAPTER`)

## 5. Node & Native Dependencies
- (None)
